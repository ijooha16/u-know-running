//예시

// import { create } from "zustand";

// const useAuthStore = create((set) => ({
//   user: null,  // 현재 로그인된 사용자 정보
//   setUser: (user) => set({ user }),  // 사용자 정보 설정
//   logout: () => set({ user: null }),  // 로그아웃
// }));

// export default useAuthStore;